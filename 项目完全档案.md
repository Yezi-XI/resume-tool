# 在线简历智能排版系统 · 项目完全档案

> 本文档完整记录项目的制作过程、功能清单、设计思路、遇到的难点与解决办法。
> 面向非程序员的读者，技术概念均用大白话解释。

---

## 一、项目概况

**这是什么**：一款在浏览器里运行的简历制作工具。左边编辑内容，右边实时预览 A4 纸效果，最后导出 PDF。全程不需要安装任何软件，打开网页就能用。

**核心指标**：
- 主开发版本（单文件 index.html）：约 2800 行代码，一个 HTML 文件装下全部功能
- 工程版本（React + TypeScript）：约 2000 行源码，模块化组织
- 内容板块：7 类（教育背景 / 实践经验 / 项目经历 / 校园经历 / 荣誉奖项 / 科研经历 / 其他信息）
- 可调样式项：100+ 个（字体、间距、边框、对齐、符号……）
- 已发布上线：GitHub Pages 托管，浏览器直接访问

**两种形态（重要设计决策）**：
| 版本 | 技术 | 用途 |
|---|---|---|
| 单文件版 | 原生 JavaScript | 主力开发版。双击即开、零依赖、便于快速迭代和发布 |
| 工程版 | React 19 + TypeScript + Vite | 早期版本，模块化架构。保留作为对照和长期演进的基础 |

**开发方式**：AI 辅助编程（vibecoding）。我负责需求定义、问题定位、方案判断和效果验证，AI 负责具体代码实现。每一个功能都经过真实浏览器自动化测试验证后才算完成。

---

## 二、整体架构与设计思路

### 2.1 内容与样式分离（最重要的一个决定）

整个工具的数据分两层：

```
简历数据（写了什么）
 ├─ 个人信息：姓名、电话、照片……
 └─ 内容板块：7 类经历，每类含多个条目
      ├─ 条目：学校/公司、职位、时间
      ├─ 任务点：小标题 + 正文（可以有很多个）
      ├─ 附加小标题：GPA、排名等短信息
      └─ 主修课程：课程名 + 成绩

样式数据（长什么样）
 ├─ 字体排版：9 大元素 × 颜色/加粗/斜体/下划线/字号/字体
 ├─ 页面设置：边距、对齐、边框、纸张装饰
 ├─ 照片设置：尺寸、位置、填充方式
 └─ 密度：紧凑/标准/宽松三档整体行距字距
```

**好处**：同一份内容可以瞬间切换三档排版密度（换行距、字距、间距而不动内容）；改样式永远不怕弄丢内容；导出 PDF 时只是换一种"渲染方式"，数据不动。

### 2.2 渲染机制：全量重绘 + 直接修改双通道

- **全量重绘**：任何内容改动后，右侧预览整个重建一遍。简单粗暴，但保证永远和编辑区一致，不会出现"改了 A 处 B 处忘了更新"的 bug。
- **直接修改（例外通道）**：拖动照片、拖滑杆这类需要"每帧跟手"的操作，如果整页重建会闪烁卡顿。这类操作改成只挪动那一个元素的位置，不动其他任何东西。

### 2.3 富文本：自创一套"标记语法"

需求是"选中几个字就能加粗"，但数据不能存成乱七八糟的网页代码。解决方案是发明了一套简单标记：

```
**加粗**    //斜体//    __下划线__
```

- 编辑时：用户选中文字点 B/I/U 按钮 → 浏览器生成格式 → 立刻转成标记存起来
- 显示时：把标记解析回带格式的文字
- 好处：数据干净、可读、不易损坏，还支持手写

### 2.4 数据持久化

所有数据自动存进浏览器的 localStorage，关掉网页再打开，内容还在。带版本号（_version），将来数据结构升级时可以做兼容迁移，老用户的数据不会坏。

---

## 三、功能全景（实现清单）

### 3.1 编辑区（左侧）

**三个标签页 + 样式面板**：

1. **个人信息**：姓名、求职意向、电话、邮箱、所在地、个人网站、照片上传、自定义字段（可增删）
2. **内容模块**：7 类板块自由添加/删除/改名；**长按拖拽**调整模块顺序（比如把科研经历提到最前面）；每个模块下条目可增删
3. **版本管理**：创建多个简历版本（如"保研-学硕版""保研-专硕版"），针对不同投递场景
4. **样式面板**：所有视觉调节集中在这里

### 3.2 内容编辑能力

- **条目排版三布局**（可全局设，也可每个模块单独设）：
  - 时间左 | 标题中 | 副标题右
  - 标题左 | 副标题中 | 时间右
  - 标题+副标题在左（竖线隔开）| 时间右
- **任务点**：一段经历里可以加任意多个"小标题：正文"组合（比如一段实习写三个工作要点），支持序号自动递增（1. 2. 3. / ①②③ 等四种格式）
- **附加小标题**（教育背景专用）：GPA、排名这类短信息，多条横向并排或纵向排列，间距按"份数"智能分配（100 份 = 正好两端对齐）
- **主修课程成绩表**：
  - 两种学术表格样式：**三线表**（顶线粗、底线粗、中间细线，一行 7 门课，课程名上、成绩下）和**网格表**（全边框，一行 4 组"课程名|成绩"）
  - 可切换成"段落模式"（课程写成一行文字）
  - 列宽自适应：默认均匀分布，某门课名字太长时那一列自动撑大，其余列均分剩余空间
- **富文本**：所有正文框支持选中文字加粗/斜体/下划线，且三种格式可以**任意叠加**

### 3.3 字体排版系统

**9 大元素**：姓名 / 求职意向 / 标题 / 副标题 / 时间 / 总起小标题 / 附加小标题 / 联系方式 / 正文 / 课程表格

每个元素独立可调：颜色、加粗、斜体、下划线、字号、字体（8 种中文字体：苹方/微软雅黑/黑体/宋体/楷体/仿宋/Arial/Georgia）。

细节设计：
- **三态开关**：默认 → 开 → 关 循环（比如加粗默认跟随正文，可以强制开或强制关）
- **恢复默认按钮**：每一项都有 ↺，一键回默认
- **数值以默认值为基准**：空值的数字框按 ↑↓ 或点步进按钮时，从"当前默认值"起步加减，而不是从最小值跳起（这是个反复打磨过的交互细节）
- **全局重置**：一键恢复所有样式

### 3.4 页面与装饰

- **纸张边框**：A4 四条边独立开关，上下粗细分开调、左右共用，颜色任意，粗细 1~40px（可以做粗装饰条）
- **顶部横线**：独立装饰线，粗细/颜色/长度（可贯穿整页）/对齐方式/到信息栏距离都可调
- **页面垂直对齐**：内容顶端对齐 / 垂直居中 / 底部对齐 / 自由排列
- **上下微调**：整页内容 ±50px 平移
- **标题下划线**：模块标题下的整行线，粗细/颜色可调，还有专门的"标题与下划线距离"微调

### 3.5 照片系统（打磨最久的部分）

- 尺寸（毫米级）、宽高比锁定
- 对齐方式：顶部 / 居中 / 底部 / **自由定位**
- 自由模式下：**直接在预览里拖动照片**，0.5mm 精度，带边界吸附（吸到内容区顶线、纸张顶边、左右边界）
- 填充方式：裁剪填满 / 完整显示 / 拉伸填满（解决"照片头顶被切"的问题）
- 置顶模式可紧贴上边框（留白 0~15mm 可调）

### 3.6 基本信息布局

- 纵向 / **横向居中**两种布局
- 姓名 + 求职意向同行（左/右或居中），底部/中心对齐可切换，间距可调
- 联系方式列间距（**支持负值**，两列可以叠近）、行间距
- 姓名下横线、板块底部横线，各自独立开关和间距
- 横向布局时联系方式一行最多 3 个，竖线分隔，相对整页（含照片）视觉居中

### 3.7 输出与质检

- **导出 PDF**：走浏览器打印通道，内联全部样式，A4 标准尺寸，模块不跨页截断
- **A4 打印检测**（自研功能）：一键体检，报告三件事——
  1. 预计打印页数
  2. 内容出界（上/下/左/右四个方向，精确到毫米）
  3. 跨页截断风险（哪些模块/条目会被从中间切开）
- **预览缩放**：40%~200%，编辑过程中随时放大看细节

### 3.8 段前符号

- 类型：符号字符（•●▪■◆›→▶▸✦➤★ 12 种）/ 图标（对勾/箭头/三角/星形/菱形/方块/圆点 8 种 SVG 矢量图标）/ 序号（1. / 1) / （1） / ①）
- 全局设置 + 每条目独立覆盖 + "跟随附加小标题"模式
- 符号颜色、间距、大小独立可调；符号变大不会把行撑高（专门做了行高隔离）

---

## 四、开发历程（五个阶段）

### 阶段一：工程版 MVP
用 React + TypeScript + Vite 搭出第一版：基本信息编辑、内容板块、预览、导出。确立了"状态管理用 zustand、数据结构分层"的骨架。

### 阶段二：转向单文件版
为了让工具"发给朋友就能用"（不用装环境、不用构建），演化出单文件版：全部代码塞进一个 HTML。实践证明这个决定对迭代速度极其有利——之后的几十轮功能都先在单文件版上做。

### 阶段三：发布上线 + 样式系统大重构
- 处理了双仓库（私有源码 + 公开网站）、构建产物一致性验证（详见难点 10）
- 发布到 GitHub Pages
- **重头戏：样式系统重构**——从零散的样式开关，重组成"9 大元素 × 6 属性"的矩阵式字体系统 + 三态开关 + 恢复默认机制
- 同期完成：A4 打印检测、个人信息头部重构（横向布局）、联系方式系列间距调节

### 阶段四：内容深化 + 交互打磨
- 新增科研经历模块、模块拖拽排序、纸张边框体系
- 主修课程表格经历了 **6 轮迭代**：两列表格 → 横向多课 → 三线表/网格表 → 纵向三线表 → 撑满宽度 → 收缩到内容宽 → 最终定稿"撑满 + 长列撑大 + 短列均分"
- 照片自由定位经历了 **约 10 轮问题修复**（详见难点 4、5）——这是全项目交互最复杂的部分

### 阶段五：教育背景内容体系
- 总起小标题、表格下方正文、多任务点、附加小标题富文本
- 富文本系统连续攻克：下划线按钮、空格保留、括号下划线、格式嵌套（详见难点 1、2、3）

---

## 五、重点难点与解决过程

> 每个案例按「现象 → 排查 → 根因 → 解决 → 验证」记录。这部分是项目里最有含金量的部分。

### 难点 1：「下划线没效果」——一次教科书级的误诊排查

**现象**：用户报告"附加小标题内容框里选中文字加括号下划线，没效果"。

**排查过程**（分层验证，每层都实测）：
1. 先测编辑器链路：输入（ABC）→ 选中 → 点 U → 检查编辑器内部结构 = 正常生成了下划线标记 ✓
2. 再测数据转换：标记互转完全正确 ✓
3. 再测预览渲染函数：手动喂数据，输出正确 ✓
4. 最后做**完整端到端测试**（模拟真实操作全流程）——发现预览区里附加小标题**整段都不存在**，不只是下划线问题

**根因**：渲染代码里，附加小标题的渲染逻辑被错误地写在"课程表格"的判断条件**里面**。也就是说：如果用户没填课程数据，附加小标题整个都不渲染。用户在编辑框里能看到自己打的字，预览里却什么都没有——所以误以为是自己最后做的"下划线"功能坏了，实际上内容从来没显示出来过。

**解决**：把附加小标题的渲染逻辑从课程表格的条件下**拆出来**，变成独立判断——有没有课程数据都照常渲染。

**启示**：用户报告的症状（下划线没效果）和真正的病因（内容根本没渲染）完全不是一回事。**逐层验证 + 端到端复现**才定位到真问题，光看症状改代码会越改越偏。

---

### 难点 2：加粗 + 下划线不能同时用 → 重写为递归解析器

**现象**：选中文字先点加粗、再点下划线，预览里只剩一种格式，另一种丢了。

**排查**：单独加粗正常，单独下划线正常，**只有叠加时才失效**——说明不是显示问题，是格式解析逻辑只认识"一层"格式。

**根因**：原来的解析逻辑是一条简单的正则匹配，只能识别"一段文字是加粗"或"一段文字是下划线"，遇到"一段文字既是加粗又套着下划线"这种嵌套结构就无能为力——外层标记识别出来后，内层标记被当成普通文字吞掉了。

**解决**：把解析器改成**递归算法**：
1. 找到第一个格式标记的起始符（比如 `**`）
2. 找到与它配对的结束符
3. 把标记**里面**的内容再扔给解析器自己处理一遍（这一步就是"递归"——函数调用自己）
4. 处理完的内层结果，被外层格式包起来
5. 标记后面的剩余文字继续同样处理

这样无论格式嵌套多少层，都能正确解析。

**验证**：单层格式、双格式叠加、三层嵌套、同段多个格式、未闭合的残缺标记（降级为普通文字不报错）——六种场景全部测试通过。

---

### 难点 3：中文排版三连击（空格消失 / 括号吃掉下划线）

这是三个独立但连续遇到的问题，都是**中文特有**的排版坑：

**3a. 空格消失**
- 现象：内容里的连续空格、首尾空格在预览里凭空消失
- 根因：网页对空格有"折叠"规则——连续多个空格只显示一个，首尾空格直接忽略。这是为英文排版设计的，中文场景（比如用空格对齐内容）就出问题了
- 解决：显示前把"连续 2 个以上的空格"和"首尾空格"替换成**不断行空格**（一种特殊空格字符，网页不敢折叠它）；单词之间的单个空格保持原样（不影响正常换行）

**3b. 括号"吃掉"下划线**
- 现象：给带全角括号的文字加下划线，下划线画到了括号笔画的下面，被遮住一部分，看起来"没显示"
- 根因：全角括号（如「（）」）的字形比普通汉字"更深"，下划线默认画在基线附近，正好被括号的下边缘盖住
- 解决：给下划线加**偏移量**（往下挪 3px）和固定线宽，让线画在所有字形（包括括号）的最低点之下

**3c. 编辑框里的下划线不显示**
- 根因：编辑框元素缺少样式类名，导致样式规则没作用上——纯粹是一个"样式选择器找不到目标"的低级但隐蔽的问题
- 解决：补上类名 + 给预览区加显式下划线规则，双保险

---

### 难点 4：照片拖动"反复横跳"——交互最难的一个 bug

**现象**：自由拖动照片到边界附近时，照片在"吸到边界"和"弹回原位"之间快速来回跳动，无法稳定。

**排查**（写测试脚本模拟了 24 次微小移动对比新旧逻辑）：
- 拖动逻辑每次移动都独立判断"离边界近不近"，近就吸过去
- 但吸附之后，光标只要往回偏一点点（越过吸附判定范围），就立刻脱离吸附弹回真实位置；再往边界方向偏一点，又吸过去——**来回触发，形成震荡**

**解决**：改用**滞回吸附**（hysteresis，工程上防抖动的经典思路）：
- 进入范围（1.5mm 内）→ 吸附并**锁定**
- 锁定后，光标在吸附点附近小幅晃动**不再重新判断**（吸附期间基准点固定）
- 只有明确拖离超过 3mm（解锁距离 > 吸附距离，两道门槛）才脱离吸附

**验证**：吸到边界后连续 24 次微小移动，位置值全程恒定不变（旧逻辑会产出 2 个来回跳的值）。彻底消除跳动。

---

### 难点 5：拖滑杆时预览闪烁卡顿

**现象**：拖动"照片位置"滑杆，右侧预览整页闪烁；慢速拖动还有跳格感；大照片时一卡一卡。

**层层根因**（一个现象背后藏了四个独立原因）：
1. 每动一下滑杆就**整页重建预览**（全量重绘的代价）→ 闪烁
2. 照片以 base64 大字符串存在数据里，**每动一下就写一次本地存储** → 大图时卡顿
3. 滑杆的滑块样式带**动画过渡效果**，按住拖动时"按下/松开"状态反复切换 → 圆点放大缩小来回闪
4. 滑杆移动触发"自动滚动到照片"，滚动导致滑杆轨道位置变化 → 取值跳变 → 又触发滚动，死循环

**对应解决**：
1. 位置类滑杆不走整页重建，改成**直接修改照片那一个元素的坐标**（预览 DOM 零重建）
2. 保存动作加 300ms **防抖**（拖动期间不写，停手才写）
3. 删掉滑块样式里的动画
4. 按住滑杆期间一律不自动滚动，松手后补一次

**验证**：连续拖动 21 次期间，重建 0 次、保存 0 次，位置实时平滑更新。

---

### 难点 6：数字框"从最小值起步"的交互缺陷

**现象**：字号等数字框留空（= 跟随默认值 10pt）时，按 ↑ 或点步进按钮，数值直接跳到最小值 6，而不是从 10 开始加。

**解决**（经历三轮方案迭代）：
1. 第一版：监听键盘 + 判断点击位置在数字框右缘 18px 内 → 用户反馈"有时还是从 6 起步"（点击位置判断在页面缩放下不可靠）
2. 第二版：改用**聚焦时静默预填**——只要聚焦到有空值的数字框，就先把当前默认值（会跟随排版密度自动变化）悄悄填进去，之后无论键盘、鼠标点箭头、滚轮，都从基准起步；没改动就失焦恢复空值
3. 补充：滑杆的滑块在空值时也要停在真实默认位置（而不是停在最小值）

**启示**：交互细节的"合理默认行为"需要专门设计，且**必须用真实浏览器 + 真实键鼠事件测试**，合成事件测不出这类问题。

---

### 难点 7：A4 打印检测的"幽灵出界"（亚像素误报）

**现象**：检测功能报告某模块"跨页截断"，肉眼看明明在同一页。

**根因**：屏幕坐标是浮点数（比如 267.0000001mm），换算页码时做除法取整，边界情况 0.2px 的浮点误差就能让上下边缘算出不同页码。

**解决**：上边缘 +0.5px、下边缘 -0.5px 的**容差**再取整，消除亚像素误差引起的误报。

---

### 难点 8：局部字号被全局字号"镇压"

**现象**：给"表格下方正文"单独设了字号，没用——只要全局正文字号一设，就把它覆盖了。

**根因**：渲染最后有一道"全局正文样式批量应用"的工序，无差别覆盖所有正文类元素，包括用户单独设过字号的那个。

**解决**：给单独设过字号的元素打一个**标记**（data-td-size），全局批量应用时看到标记就跳过字号这一项（颜色、加粗等其他全局样式照常应用）。

---

### 难点 9：一个数字引发的"整面板消失"

**现象**：改一个间距功能后，整个内容编辑面板空白消失。

**根因**：界面构建函数要求传入的文字参数必须是字符串，代码里误传了一个**数字**，函数在拼接时直接报错，而面板是整体构建的——一处报错，全军覆没。

**解决与规矩**：数字必须显式转字符串。并由此立下项目铁律：**任何改动不能只做语法检查，必须开真实浏览器验证界面真的渲染出来了**。这条规矩后来至少避免了三次同类事故。

---

### 难点 10：两个版本"到底一不一样"——构建产物逐字节验证

**背景**：项目有本地和远端两个分叉的代码版本，都要判断"运行起来到底有没有区别"，凭肉眼看代码差异根本不可靠（引号风格、转义方式等表象差异太多）。

**方法**（这一段体现了实证精神）：
1. 用 git worktree 把远端版本单独检出、单独构建
2. 为了不重复下载依赖，用 Windows 目录联接（junction）共享依赖目录
3. **两个版本各自构建，产物做 MD5 逐字节比对 → 完全一致**
4. 不放心，又做了反向验证：往源码里注入一个标记字符串再构建 → 标记确实出现在产物里，证明构建过程读的确实是这份源码，不存在路径错乱
5. 结论：两版差异纯属代码风格表象，**运行时功能完全一致**——据此做出保留哪一版的决策

---

## 六、测试与质量保障

**测试方法**：Playwright + Edge 无头浏览器的端到端自动化测试。

**具体做法**：
- 每个功能做完，写自动化脚本模拟真实用户操作（点击、输入、拖拽、按键）
- 直接在真实浏览器里跑，断言渲染结果（检查页面元素、计算样式、实际尺寸）
- 特殊场景：切换排版密度、空值默认行为、旧数据兼容、边界值夹紧
- 配合语法检查（node --check）做第一道防线

**测试纪律（用事故换来的）**：
1. 语法检查通过 ≠ 功能正常，必须浏览器实测
2. 同一文件的多处修改不并行执行（会互相覆盖），串行进行
3. 测试脚本自己的单位要搞清楚（曾把像素当毫米传参，导致两轮测试全假通过）
4. 合成事件 ≠ 真实事件（滑杆这类交互必须真实键鼠模拟）

**数据兼容设计**：所有新字段都设计成"缺省 = 老行为"，旧数据加载后自动补齐默认值，从不需要用户做任何迁移操作。

---

## 七、我的角色（AI 辅助开发的工作流）

这个项目采用 vibecoding 模式完成。我实际承担的工作：

1. **需求定义与拆解**：把"我想要一个好看的简历"拆解成一个个明确的具体需求（"三线表一行放 7 门课""间距按份数分配""吸附要有防抖"）
2. **视觉验收**：每轮改动后看实际效果，判断"对/不对/差在哪"
3. **问题定位**：复现 bug、描述症状、提供线索（"只有照片在顶部时缺失，往下移就完整"——这类线索直接决定排查方向）
4. **方案权衡**：在多个技术方案里做选择（比如表格列宽的三种策略，实测后拍板"撑满+长列撑大+短列均分"）
5. **效果验证**：确认自动化测试通过、视觉符合预期，才算完成

AI 承担代码实现，但**以上五项全部由我完成**——其中问题定位和方案权衡是保证项目质量的关键：很多 bug（难点 1、4、5）如果只按表面症状改，永远修不好。

---

## 附：项目文件结构

```
resume-tool/
├─ index.html          # 单文件版（主力开发版，约 2800 行）
├─ resume-tool/         # React + TypeScript 工程版
│  ├─ src/
│  │  ├─ components/
│  │  │  ├─ editor/    # 编辑器组件（个人信息/板块/条目/样式/版本）
│  │  │  └─ preview/   # 预览渲染组件
│  │  ├─ store/        # 状态管理（zustand）
│  │  └─ types/        # 数据类型定义
│  └─ standalone.html  # 早期单文件快照
└─ .workbuddy/memory/  # 完整开发日志（每天每轮的记录）
```
